name: yuvi_audio_engine
description: YuVi Rays audio engine — Dart wrappers for the Web Audio API / Tone.js bridge.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
