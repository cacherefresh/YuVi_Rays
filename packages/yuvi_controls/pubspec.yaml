name: yuvi_controls
description: YuVi Rays UI controls — reusable knobs, faders, transport buttons, and audio graph widget.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  yuvi_audio_engine:
    path: ../yuvi_audio_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
